# Archivos y carpetas que Docker NO debe copiar al contexto de build.
# Reduce el tamaño del contexto enviado al daemon y evita meter basura
# de tu máquina local (builds previos, IDE, git) dentro de la imagen.

target/
.git/
.gitignore
.idea/
*.iml
README.md
